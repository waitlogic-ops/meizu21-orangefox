# Meizu 21 OrangeFox R3：APEX 与解密服务诊断候选

**不是已证明可启动的修复版。R2 已由用户反馈仍卡屏。** 本版应生成 `OrangeFox-meizu21-R3-DIAGNOSTIC.img`，仅用于标准版 M2461/meizu21 的已授权适配。未进行实体机测试。

## 本轮范围

用户确认同时参考 21 Pro 的解密服务方案，因此本版不再是只改变 APEX 的单变量实验。

- 禁用 APEX，匹配用户可启动旧版及 21 Pro 参考。
- QSEE class 改 core，KeyMint/Gatekeeper 改 root、early_hal，三者库搜索路径匹配 Pro（包含 /sbin）。服务仍使用魅族 21 原厂提取组件。
- Gatekeeper 仅等待 QSEE listeners；KeyMint 独立响应版本属性准备完成，移除它对 listeners 的额外门槛。
- 增加 vendor.gatekeeper.disable_spu=true，保留 is_security_level_spu=0。
- **区别于 Pro 直接在 modules.loaded 时启动 KeyMint 的脚本**：保留实际系统版本/补丁读取后再启动 KeyMint，避免照搬脚本注释未保证的属性准备。读取失败仍使用先前国际版回退值并记录错误，不能视为版本匹配已验证。
- 保留最多 30 秒等待进程的保护；这不是 Binder 就绪检测，也不是对解密调用本身的超时保护。
- 添加启动阶段屏幕编号及日志，增加 strace/debuggerd。保留 R2 的 MTP、原厂节点权限、标准版驱动和分区参数；不复制 Pro 驱动、不伪造未来补丁值、不修改原固件。

参考设备树固定版本：
https://github.com/adontoo/device_meizu_m2481-TWRP/tree/c7cea8004036ca55bc3fce125080aa0ad7c28bac

## 阶段编号

左上角橙色两位编号用像素绘制，不依赖主界面字体。编号表示**进入**相应阶段；停留在那里不证明该阶段就是根因，也可能是编号绘制/刷新本身未完成。日志记录 `MEIZU21_STAGE`、PID 和单调时钟时间；进程反复重启通常可从不同 PID 和重复序列识别。

| 编号 | 位置 |
|---|---|
| 01 | 解析 fstab 之前，仅日志，图形尚未初始化 |
| 02 | 图形初始化之前，仅日志 |
| 10 / 11 | 触摸输入初始化开始 / 返回 |
| 20 | 分区后处理开始 |
| 21 / 22 | 解锁块分区阶段开始 / 返回 |
| 30 | 为后续流程挂载 system/vendor 开始 |
| 31 | 已到达禁用 APEX 分支（某些挂载条件不满足时不经过此编号） |
| 32 / 33 | APEX 开始 / 返回：为以后对照保留源码标记，本版关闭 APEX，不应运行 |
| 40 | system/vendor 卸载阶段开始 |
| 41 | 设置存储准备阶段开始 |
| 50 / 51 | 系统版本属性读取开始 / 完成并发布 props_ready |
| 60 | 等待解密服务进程 |
| 61 | 等待超时，跳过启动阶段自动解密 |
| 70 / 71 | Decrypt_Data 开始 / 返回 |
| 80 | 更新系统/分区信息开始 |
| 81 | 挂载清理完成，随后仍有槽位等收尾操作 |
| 90 / 91 | 主界面资源加载开始 / 返回 |

91 后停止显示诊断叠层，后续界面会覆盖已有编号。91 并不表示用户界面已经正常可操作；后面的设置加载、解密页面和交互仍可能失败。

## 验证与限制

离线检查包括补丁覆盖、重复应用、上游变化拒绝、绘图调用边界、服务配置、解密等待分支、MTP 触发条件。云端还需真实编译并检查实际镜像中的 APEX 禁用标记、阶段文本、服务身份、SPU 属性、ELF 依赖与 AVB。

显示阶段号本身会轻微改变启动时序；没有真实启动日志或用户观察，不能确定因果。因为本轮同时改了 APEX 和服务方案，即便启动改善，也不能只归功于其中一项。

本轮不执行任何手机命令。刷入与回滚原则参照 TESTING.md，镜像文件名为 `OrangeFox-meizu21-R3-DIAGNOSTIC.img`。保留原厂 recovery 及用户可启动旧版；不格式化 data/metadata、不修改 vbmeta，不执行 fastboot boot（镜像无内核）。

## 构建依赖修正

运行 34398456503 完成编译，但镜像检查发现新增 debuggerd 缺少 libdebuggerd_client.so 和 libprocinfo.so，因此未发布产物。后续构建将两库加入 recovery 的编译与复制列表；仍须以实际镜像依赖检查通过为准。该问题不能解释此前 R1/R2 的卡屏。
